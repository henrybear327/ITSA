#include <cstdio>

int main()
{
    int ncase;
    scanf("%d", &ncase);

    while(ncase--) {
	int n;
	scanf("%d", &n);
	
	char input[100];
	for(int i = 0; i < 5; i++) {
	    fgets(input, 100, stdin);
	    for(int j = 0; j < 5; j++) {
		if()
	    }		
	}
    }

    return 0;
}
